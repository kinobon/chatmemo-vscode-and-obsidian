{
  "name": "chatmemo-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "pnpm --filter chatmemo run build",
    "build:obsidian": "pnpm --filter @chatmemo/obsidian run build",
    "build:all": "pnpm -r run build",
    "package": "pnpm --filter chatmemo run package"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
